# 停止后将操作结果留到下一条用户输入

Status: implemented
Translation: pending

## 摘要

停止过去只取消当前回合，已积压的不同操作仍会逐条续聊。现在用户停止会持久标记旧操作及其来源，重启和迟到完成都不会解除阻挡。下一条实际提交的用户消息会一次携带已就绪结果，包括已有队列中的用户消息。界面显示当前请求者的待处理数量，支持本次不携带或手动处理；安装包验收和跨机器迁移不在本次验证范围内。

## 依据与决策

基线 `3772961c` 的 coordinator 使用每会话 Promise 链逐条执行 Delivery，`cancelSession` 只取消精确匹配的回合。现有执行认领防止单条重复执行，但无法阻止不同通知在停止后继续启动。编辑并重发、MCP 单操作取消也经过取消路径，所以只给用户 Stop 增加持久阻挡；内部取消通过 `--turn-only` 保留原义。

行为见 [草案](../../../../specs/operation-delivery-control.zh.md)，实现职责与兼容边界见 [编排 README](../../../../apps/cli/src/orchestration/README.md#deferred-results)。这是现有逐操作执行认领之上的补充，不改变委派深度限制或目标任务生命周期。

早期提案要求额外点击恢复。改为下一条用户输入携带结果，避免用户补充指令时还需切换状态；同时不重新放开整条旧队列。旧操作的迟到结果继续等待，新来源回合发起的工作仍自动续聊。

## 持久边界

- SQLite 侧表保存停止来源、持有的操作和用户输入收据。停止与接受操作均在立即写事务中完成，插入触发器覆盖停止后的迟到接受和旧完成回合的后续操作。
- UI 保存本次是否携带结果的选择，队列提升、普通 dispatch 和 steer 保留选择；实际提交时认领已就绪结果，不区分消息在停止前还是停止后入队。
- provider 调用前同步认领结果并记录 started；一个用户输入只有一个收据，claim 使用随机标识防止旧拒绝回调释放新尝试。确认 steer 未提交时释放；取消已开始输入不重放；未知结果保留 uncertain。
- 结算失败保留确切结果并安排仅结算重试，不再次调用代理。暂停写失败时保留进程内门禁、取消当前回合并返回失败，不能宣称重启后仍有效。
- 已有 progress 卡片独立更新；暂停不追加 completion 回合。元数据只投影按请求者计数，跳过相同值，不能作为执行权来源。

## 输入与兼容性

输入使用当前用户的模型与指令，旧结果明确标为参考资料。较大积压保留结构、目标历史引用和截断信息，每次最多 68 KiB；仍放不下的结果保留待处理，不能自动拆成多轮。原始操作结果继续保存在数据库，已消费结果沿用七天保留策略，未消费暂停结果不因自动投递过期规则而消失。

新协议声明 `deferredOperationInputs` v1。内部 CLI 取消仅在目标支持时发送 `turnOnly` 字段，旧守护进程仍收到原来的 payload。普通用户输入不需要额外编号。持久保证限于同一机器的重启，降级旧二进制或跨机器迁移不会自动继承执行门禁。

## 验证与后续

显式 Stop 的目标回合已结束时，允许停止接替启动的自动完成回复：历史必须包含目标 Assistant，其后的当前来源必须是 operation completion，且期间不能出现用户输入。读取历史后重新检查当前 runtime 与因果来源，再同步持久暂停并取消当前回合；普通 turn-only 取消仍精确匹配。回归覆盖自动接替、新用户边界、读取历史期间发生人类接管、未知目标和内部取消，不引入停止编号。执行服务 94 项测试及完整 `pnpm check` 通过。

按用户反馈移除本 PR 引入的停止编号，以及数据库计数、元数据投影和跨层透传。已有队列消息可以携带结果，不再额外限制必须由停止后新写的消息触发；对应数据库和状态模型测试改为验证这一行为。队列提升测试仍检查携带选择的保留。

移除编号后，相关 267 项测试及完整 `pnpm check` 通过。本次只简化结果携带条件；renderer 内部取消与显式 Stop 共用意图的复查问题仍需单独修复。

覆盖停止后重开数据库、准备与启动之间停止、迟到接受/完成、新旧来源隔离、已有排队消息携带结果、输入认领幂等、旧回调 ABA、崩溃 uncertain、大结果输入界限、失败结算重试，以及真实执行服务的输入合并和内部取消隔离。有限状态模型增加停止与用户输入绑定，并枚举有界交错。界面交互测试覆盖计数、取消携带和手动处理，Storybook 提供等待、排除、忙碌和空状态。

在独立临时检出中执行完整 `pnpm check`，类型、静态检查、测试和公共边界检查通过；其中组件 3276 项、CLI 2630 项测试通过，CLI 4 项既有测试跳过。随后补充重连时冻结空/非空结果集合、已确认 steer 结算原输入的断言，执行服务 88 项测试、CLI 类型检查和改动文件静态检查再次通过。工作区 `pnpm run docs check` 与 `git diff --check` 通过。

当前环境没有可用浏览器，界面完成组件交互测试但未做视觉验收；没有进行真实 provider 消费或安装包测试，也没有证明最初积压产生的原因。正常运行时的自动完成批次合并仍是独立后续优化。
